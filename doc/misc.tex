\section{Misc}

\subsection{To Do}

\begin{itemize}
 \setlength{\parskip}{0cm}
 \setlength{\itemsep}{0cm}
 \item Trace/Overhead計測ツール実装(大体OK)
 \item run\_tq等を構造体のメンバ変数にすることで，モノリシックカーネル
   でもキャッシュライン毎にアクセスできるようにする．
 \item CPU周波数を動的に取得する．\verb|CPU_CLOCK_MHZ_PER_USEC|から変
   更する．
 \item x86/ARMでMMU/Cacheを設定する．
 \item Mcube KernelをLinuxに適用することを検討する．
\end{itemize}


\subsection{Known Issues}

\begin{itemize}
 \setlength{\parskip}{0cm}
 \setlength{\itemsep}{0cm}
 \item x86 clangで\verb|-O3|でコンパイルすると起動しない．
  \verb|-O2|以下はOK．
\end{itemize}

\subsection{Coding Styles}

\begin{itemize}
 \setlength{\parskip}{0cm}
 \setlength{\itemsep}{0cm}
 \item 移植性はできる限り高く
 \item マクロは全部大文字にする
 \item プログラム中に参考文献の論文を書く
 \item \verb|a != nullptr -> a|, \verb|a == nullptr -> !a|
 \item 改行する場合，演算子(\verb|+-*/|等)は行の先頭に書く
 \item classの初期化は\verb|{}|を利用
 \item メンバ関数の宣言にはoverride, finalを利用
 \item templateメンバ関数のリンクエラーの場合は定義と宣言を同じにする
 \item Doxygenで変数は@a，予約語は@cを利用
 \item \verb|NULL -> nullptr|
 \item \verb|i++ -> ++i|
 \item グローバル変数は大文字から始める
\end{itemize}
